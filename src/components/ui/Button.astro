---
type Variant = 'primary' | 'secondary' | 'ghost' | 'link';

interface Props {
  href?: string;
  target?: '_blank' | '_self';
  rel?: string;
  variant?: Variant;
  fullWidth?: boolean;
  ariaLabel?: string;
  title?: string;
  disabled?: boolean;
  className?: string;
}

const {
  href,
  target = '_self',
  rel = target === '_blank' ? 'noreferrer noopener' : undefined,
  variant = 'primary',
  fullWidth = false,
  ariaLabel,
  title,
  disabled = false,
  className = '',
} = Astro.props;
const classes = ['btn', `btn-${variant}`, fullWidth ? 'btn-full' : '', disabled ? 'is-disabled' : '', className].filter(Boolean).join(' ');
---

{
  href && !disabled ? (
    <a class={classes} href={href} target={target} rel={rel} aria-label={ariaLabel} title={title}>
      <slot />
    </a>
  ) : (
    <button class={classes} type="button" aria-label={ariaLabel} title={title} disabled={disabled}>
      <slot />
    </button>
  )
}

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--inline-gap);
    position: relative;
    overflow: hidden;
    border-radius: var(--radius-pill);
    border: 1px solid transparent;
    padding: 0.72rem 1.15rem;
    font-weight: 600;
    letter-spacing: 0.01em;
    transition:
      transform var(--motion-fast) var(--motion-ease),
      box-shadow var(--motion-fast) var(--motion-ease),
      border-color var(--motion-fast) var(--motion-ease),
      background var(--motion-fast) var(--motion-ease),
      color var(--motion-fast) var(--motion-ease);
  }

  .btn::before {
    content: '';
    position: absolute;
    inset: 0;
    transform: translateX(-120%) skewX(-24deg);
    background: linear-gradient(95deg, transparent, rgba(255, 255, 255, 0.36), transparent);
    transition: transform 620ms var(--motion-ease);
    pointer-events: none;
  }

  .btn:hover {
    transform: translateY(-2px);
  }

  .btn:hover::before {
    transform: translateX(120%) skewX(-24deg);
  }

  .btn:active {
    transform: translateY(0) scale(0.98);
  }

  .btn.is-disabled,
  .btn:disabled {
    cursor: not-allowed;
    transform: none;
    box-shadow: none;
  }

  .btn.is-disabled::before,
  .btn:disabled::before {
    display: none;
  }

  .btn-full {
    width: 100%;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--accent), var(--accent-strong));
    color: #fff;
    box-shadow: 0 12px 26px rgba(16, 74, 168, 0.42);
  }

  .btn-primary:hover {
    box-shadow: 0 16px 30px rgba(14, 65, 148, 0.48);
  }

  :global([data-theme='light']) .btn-primary {
    background: linear-gradient(140deg, #0d1d4a 0%, #1748b6 56%, #0c8a92 144%);
    border-color: color-mix(in srgb, var(--accent-strong) 68%, rgba(255, 255, 255, 0.14));
    box-shadow:
      0 22px 34px rgba(13, 29, 74, 0.2),
      0 8px 18px rgba(12, 138, 146, 0.12),
      inset 0 1px 0 rgba(255, 255, 255, 0.14);
  }

  :global([data-theme='light']) .btn-primary:hover,
  :global([data-theme='light']) .btn-primary:focus-visible {
    box-shadow:
      0 26px 40px rgba(13, 29, 74, 0.24),
      0 10px 22px rgba(12, 138, 146, 0.16),
      inset 0 1px 0 rgba(255, 255, 255, 0.18);
  }

  .btn-secondary {
    background: color-mix(in srgb, var(--card) 88%, transparent);
    border-color: color-mix(in srgb, var(--stroke) 92%, transparent);
    color: var(--text-0);
  }

  :global([data-theme='light']) .btn-secondary {
    background:
      radial-gradient(circle at 16% 18%, rgba(255, 255, 255, 0.84), transparent 30%),
      linear-gradient(160deg, rgba(255, 255, 255, 0.99), rgba(246, 249, 253, 0.94) 58%, rgba(238, 244, 250, 0.95) 100%);
    border-color: color-mix(in srgb, var(--stroke) 96%, rgba(255, 255, 255, 0.76));
    box-shadow:
      0 12px 20px rgba(15, 28, 46, 0.07),
      inset 0 1px 0 rgba(255, 255, 255, 0.88);
  }

  .btn-secondary:hover,
  .btn-secondary:focus-visible {
    background: color-mix(in srgb, var(--card) 96%, color-mix(in srgb, var(--accent) 8%, transparent));
    border-color: var(--stroke-strong);
  }

  .btn-secondary.is-disabled,
  .btn-secondary:disabled {
    background: color-mix(in srgb, var(--card) 78%, transparent);
    border-color: color-mix(in srgb, var(--stroke) 92%, transparent);
    color: var(--muted);
    opacity: 0.82;
  }

  .btn-ghost {
    background: color-mix(in srgb, var(--card) 44%, transparent);
    color: var(--card-text-0);
    border-color: color-mix(in srgb, var(--stroke) 92%, transparent);
  }

  :global([data-theme='light']) .btn-ghost {
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.78), rgba(243, 248, 253, 0.6));
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.72);
  }

  .btn-ghost:hover,
  .btn-ghost:focus-visible {
    background: color-mix(in srgb, var(--card) 78%, color-mix(in srgb, var(--accent) 10%, transparent));
    color: var(--text-0);
    border-color: var(--stroke-strong);
  }

  .btn-ghost.is-disabled,
  .btn-ghost:disabled {
    color: var(--muted);
    border-color: color-mix(in srgb, var(--stroke) 88%, transparent);
    opacity: 0.78;
  }

  .btn-link {
    border: 0;
    padding: 0;
    color: var(--card-text-0);
    border-radius: 0;
    text-decoration: underline color-mix(in srgb, var(--accent) 24%, transparent);
    text-underline-offset: 4px;
    overflow: visible;
  }

  .btn-link::before {
    display: none;
  }

  .btn-link:hover,
  .btn-link:focus-visible {
    transform: none;
    color: var(--text-0);
    text-decoration-color: var(--accent-2);
  }
</style>
